services:
  nginx:
    image: nginx:latest
    restart: unless-stopped
    ports:
      - "80:80"
    networks:
      - nginx_network
    volumes:
      - ./conf.d:/etc/nginx/conf.d

networks:
  nginx_network:
    driver: bridge
